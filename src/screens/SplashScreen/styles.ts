import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    imgContainer : {
        width : '100%',
        height : undefined,
        aspectRatio : 1,
        alignItems : 'center',
        justifyContent : 'center'
    },
    img : {
        width : '80%',
        height : undefined,
        aspectRatio : 1
    },
});